#include "console.h"
#include "server.h"
#include <stdio.h>

int main() {
    /*char *cmd = NULL;
    while ((cmd = read_cmd()) != NULL) {
        printf("receive: %s\n", cmd);
    }
    */
    start();
}
